services:
  front:
    build:
      context: .
    ports:
      - "5173:5173"
    volumes:
      - .:/Critical_Blox
      - /Critical_Blox/node_modules
    env_file:
      - .env
    command: npm run dev -- --host 0.0.0.0
